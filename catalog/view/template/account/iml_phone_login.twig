{{ header }}
<div id="account-login" class="container">
	<ul class="breadcrumb">
		{% for breadcrumb in breadcrumbs %}
			<li class="breadcrumb-item">
				<a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a>
			</li>
		{% endfor %}
	</ul>
	<div class="row">{{ column_left }}
		<div id="content" class="col">{{ content_top }}

			<div class="row">
				<div class="col mb-3">
					<div class="border rounded p-3 d-flex flex-column h-100 ">
						<form id="form-login" action="{{ sentOtp }}" method="post">
							<h2>{{ text_returning_customer }}</h2>
							<p>
								<strong>{{ text_i_am_returning_customer }}</strong>
							</p>
							<div class="row mb-3">
								<label for="input-email" class="col-sm-2 col-form-label"><strong>E-Mail Address</strong></label>
								<div class="col-sm-10">
									<input type="text" name="email" value="" placeholder="E-Mail Address" id="input-email" class="form-control">
								</div>
							</div>
							<div class="row mb-3">
								<label for="phone" class="col-sm-2 col-form-label"><strong>Phone Number</strong></label>
								<div class="col-sm-10">
									<input type="tel" id="phone" name="phone" class="form-control" placeholder="Enter phone number with country code" required>
								</div>
							</div>

							<div class="text-end">
								<button type="submit" class="btn btn-primary" id="btn-send">Send OTP</button>
							</div>
							{% if redirect %}
								<input type="hidden" name="redirect" value="{{ redirect }}"/>
							{% endif %}
						</form>
					</div>
				</div>
			</div>

			{{ content_bottom }}</div>
		{{ column_right }}</div>
</div>
{{ footer }}

<script type='text/javascript'>

	// $('#btn-send').on('click', function(e){
	// 	var element = this;

	// 	$.ajax({
	// 		url: 'index.php?route=extension/iml_phone_login/account/iml_phone_login.sendOtp&language={{ language }}',
	// 		type: 'post',
	// 		data: new FormData($('#form-login')[0]),
	// 		dataType: 'html',
	// 		beforeSend: function () {
	// 			$(element).button('loading');
	// 		},

	// 		complete: function () {
	// 			$(element).button('reset');
	// 		},

	// 		success : function(html) {
	// 		$('.mobile_content').html();
	// 		$('#btn-send').html("<a href='#' class='btn btn-primary' id='new-register'>Continue</a>");
	// 		},
	// 		error : function(xhr, ajaxOptions, thrownError) {
	// 			alert(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
	// 		}

	// 	});
	// })
// 	$('#btn-send').on('click', function () {
// 		$number = $("#phone").val():

// 		console.log($number);
// 		return;


// 		var element = this;
// 		$.ajax({
// 			url: 'index.php?route=extension/iml_sizechart/common/iml_sizechart.sentOtp',
// 			type: 'post',
// 			data: {phone : $number},
// 			dataType: 'json',
// 			beforeSend: function () {
// 			$(element).button('loading');
// 			},

// 			complete: function () {
// 			$('.coupon_btn').button('reset');
// 			},

// 			success: function (json) {
// 			location.reload();
// 			}

// 		});
// 		})

</script> 
